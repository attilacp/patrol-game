// chat/chat-system.js
class ChatSystem {
    sendMessage(message) {
        // Mensagens em tempo real
    }
    
    sendGameEvent(event) {
        // Eventos do jogo: "João acertou!", "Time Azul errou!"
    }
}