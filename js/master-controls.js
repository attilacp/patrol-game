// game/master-controls.js
class MasterControls {
    startGameForAll() {
        // Mestre inicia jogo para todos
    }
    
    controlQuestionFlow() {
        // Apenas mestre avança perguntas
    }
    
    broadcastAnswer(result) {
        // Transmitir resultado para todos
    }
}