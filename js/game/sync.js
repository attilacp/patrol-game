// game/sync.js
class GameSync {
    syncGameState(gameState) {
        // Sincronizar: pergunta atual, pontuações, equipe de plantão
    }
    
    syncQuestion(questionIndex) {
        // Todos veem a mesma pergunta
    }
    
    syncScore(teamId, score) {
        // Atualizar pontuação em tempo real
    }
}